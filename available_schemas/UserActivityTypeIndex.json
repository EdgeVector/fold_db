{
  "name": "UserActivityTypeIndex",
  "key": {
    "hash_field": "activity_type",
    "range_field": "timestamp"
  },
  "transform_fields": {
    "activity_type": "UserActivity.map().activity_type",
    "user_id": "UserActivity.map().user_id",
    "timestamp": "UserActivity.map().timestamp",
    "activity_id": "UserActivity.map().activity_id",
    "resource_id": "UserActivity.map().resource_id",
    "metadata": "UserActivity.map().metadata",
    "ip_address": "UserActivity.map().ip_address"
  }
}

